# Quiz Solver — Starter


This repo implements a FastAPI endpoint that accepts quiz tasks and solves them by visiting JavaScript-rendered quiz pages using Playwright.


## Quick start


1. Create a venv and install requirements:


```bash
python -m venv .venv
source .venv/bin/activate # or .venv\Scripts\activate on Windows
pip install -r requirements.txt
playwright install